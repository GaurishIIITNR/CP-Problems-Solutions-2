#include <bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    ll t = 1;
    cin >> t;
    while (t--)
    {
        ll n;
        cin >> n;
        string s,s1;
        cin >> s>>s1;
        ll res = 0;
        for (ll i = 0; i < s.length(); i++)
        {
            if(s[i]>=s1[i]){
                ll p=s1[i]-s[i];
                ll p1=26-p;
                if(p)
            }
        }
    }
}