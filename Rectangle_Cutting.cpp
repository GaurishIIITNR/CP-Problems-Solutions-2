#include <bits/stdc++.h>
using namespace std;
#define ll long long int

ll f(ll a,ll b){
    // Base

    // Recursive
    ll mx=0;
    for(ll i=1;i<=min(a,b);i++){
     mx=max(mx,1+f(a-))
    }
}
int main()
{
    ll a, b;
    cin >> a >> b;
   
}